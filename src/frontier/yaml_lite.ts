/** Placeholder: Kirby configs load via JSON mirrors of YAML. */
export function parseYamlLite(_text: string): unknown {
  throw new Error('Use JSON config mirrors for runtime; YAML is the normative human source.');
}
